<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="sfr.com.tasker">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/device"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher">
        <activity android:name="sfr.com.tasker.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="sfr.com.tasker.AddTodoItem"
            android:parentActivityName="sfr.com.tasker.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="sfr.com.tasker.MainActivity"/>
        </activity>
        <activity
            android:name="sfr.com.tasker.DetailTodoItem"
            android:parentActivityName="sfr.com.tasker.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="sfr.com.tasker.MainActivity"/>
        </activity>

        <receiver android:name="sfr.com.tasker.Utils.NotificationPublisher"/>

        <activity android:name="sfr.com.tasker.AboutActivity"
                  android:parentActivityName="sfr.com.tasker.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="sfr.com.tasker.MainActivity"/>
        </activity>
    </application>

</manifest>